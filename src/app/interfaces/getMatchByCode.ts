export interface GetMatchByCode {
    idMatchDTO: string;
    teamADTO: string;
    teamBDTO: string;
    scoreTeamADTO: string;
    scoreTeamBDTO: string;
    dateTimeMatchDTO: Date;
    enabledDTO: Date;
}
